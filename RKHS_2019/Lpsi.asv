function [dpsidlon,dpsidlat] = Lpsi(beta,K,dlon,dlat)
n        = size(beta,2);
dpsidlon = zeros(n,n);
dpsidlat = zeros(n,n);
for i = 1:n
    for j = 1:i-1
        dKd
        dpsidlon(:,i) = dpsidlon(:,i) + beta(i,j) .* LK;
    end
end